import PagesHeader from '../../components/shared/PagesHeader';

function Teams() {
  return (
    <div>
      <PagesHeader headerText={'Team'} subText={null} />
    </div>
  );
}
export default Teams;
